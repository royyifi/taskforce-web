"use client";

import { useEffect, useState } from "react";
import Link from "next/link";
import { ArrowLeft, ShieldCheck, LogOut, CheckCircle2, XCircle, Clock3, Users, Activity, AlertCircle, RefreshCw, PencilLine } from "lucide-react";
import TeamAdmin from "@/components/team-admin";
import AccountAdmin from "@/components/account-admin";
import ChangePassword from "@/components/change-password";

interface Stats { totalPartners:number; pendingPartners:number; pendingActivities:number; totalActivities:number; partnersUsed:number; partnersUnused:number; agreementsEndingSoon:number; partnersWithoutPic:number }
interface PendingPartner { id:string; name:string; level:string; category:string|null; createdAt:string }
interface PendingActivity { id:string; title:string; type:string; partnerName:string; submittedBy:string|null; createdAt:string; lecturers?: { name:string; isLeader:boolean }[]; students?: { name:string }[] }
interface PendingEdit { id:string; partnerId:string; partnerName:string; submitterName:string; submitterEmail:string; note:string|null; createdAt:string; changes:Record<string,string> }

const FIELD_LABELS: Record<string,string> = { name:"Nama", level:"Level", category:"Kategori", address:"Alamat", phone:"Telepon", email:"Email", website:"Website", picName:"Nama PIC", picPosition:"Jabatan PIC", picPhone:"Telepon PIC", picEmail:"Email PIC", city:"Kota", country:"Negara" };

export default function AdminPage() {
  const [stats, setStats] = useState<Stats|null>(null); const [partners, setPartners] = useState<PendingPartner[]>([]); const [activities, setActivities] = useState<PendingActivity[]>([]); const [edits, setEdits] = useState<PendingEdit[]>([]); const [role, setRole] = useState(""); const [loading, setLoading] = useState(true); const [error, setError] = useState("");
  async function load() { setLoading(true); const res = await fetch("/api/admin/stats"); if (res.status === 401) { window.location.href = "/login"; return; } const data = await res.json(); setStats(data.kpi); setPartners(data.pendingPartners || []); setActivities(data.pendingActivitiesList || []); setEdits(data.pendingEdits || []); fetch("/api/auth/session").then(r => r.json()).then(d => setRole(d.user?.role || "")); setLoading(false); }
  useEffect(() => { load(); }, []);
  async function action(url:string, body:object) { setError(""); const res = await fetch(url, { method:"PATCH", headers:{"Content-Type":"application/json"}, body:JSON.stringify(body) }); if (!res.ok) { setError("Aksi tidak berhasil."); return; } load(); }
  async function logout() { await fetch("/api/auth/logout", { method:"POST" }); window.location.href = "/"; }
  if (loading && !stats) return <div className="flex min-h-[calc(100vh-4rem)] items-center justify-center bg-stone-50"><RefreshCw className="h-6 w-6 animate-spin text-emerald-600" /></div>;
  return <div className="min-h-[calc(100vh-4rem)] bg-stone-50"><header className="border-b border-stone-200 bg-white"><div className="mx-auto flex max-w-7xl items-center justify-between px-4 py-5"><div><div className="flex items-center gap-2"><ShieldCheck className="h-5 w-5 text-emerald-700" /><h1 className="text-xl font-bold text-stone-900">Dashboard Admin</h1>{role === "SUPER_ADMIN" && <span className="rounded-full bg-emerald-600 px-2.5 py-1 text-[10px] font-bold uppercase tracking-wider text-white">Super Admin</span>}{role === "TEAM_ADMIN" && <span className="rounded-full bg-blue-50 px-2.5 py-1 text-[10px] font-bold uppercase tracking-wider text-blue-700">Admin Tim</span>}</div><p className="mt-1 text-sm text-stone-500">Kelola data dan verifikasi pengajuan Taskforce.</p></div><div className="flex items-center gap-2"><Link href="/" className="hidden rounded-lg border border-stone-200 px-3 py-2 text-xs font-semibold text-stone-600 hover:bg-stone-50 sm:inline-flex"><ArrowLeft className="mr-1.5 h-3.5 w-3.5" /> Portal Publik</Link><button onClick={logout} className="inline-flex items-center gap-1.5 rounded-lg border border-red-100 px-3 py-2 text-xs font-semibold text-red-600 hover:bg-red-50"><LogOut className="h-3.5 w-3.5" /> Keluar</button></div></div></header><main className="mx-auto max-w-7xl space-y-8 px-4 py-8">{error && <div className="flex items-center gap-2 rounded-lg bg-red-50 p-3 text-sm text-red-700"><AlertCircle className="h-4 w-4" />{error}</div>}<section className="grid grid-cols-2 gap-3 lg:grid-cols-4"><Stat icon={Clock3} label="Pending Mitra" value={stats?.pendingPartners || 0} color="amber" /><Stat icon={Activity} label="Pending Kegiatan" value={stats?.pendingActivities || 0} color="blue" /><Stat icon={Users} label="Mitra Belum Digunakan" value={stats?.partnersUnused || 0} color="yellow" /><Stat icon={AlertCircle} label="PKS ≤ 90 Hari" value={stats?.agreementsEndingSoon || 0} color="red" /></section><div className="grid gap-8 lg:grid-cols-2"><section className="rounded-2xl border border-stone-100 bg-white shadow-sm"><div className="flex items-center justify-between border-b border-stone-100 p-5"><div><h2 className="font-bold text-stone-900">Usulan Mitra Baru</h2><p className="mt-0.5 text-xs text-stone-500">Perlu ditinjau dan diverifikasi</p></div><span className="rounded-full bg-amber-50 px-2.5 py-1 text-xs font-bold text-amber-700">{partners.length} pending</span></div>{partners.length ? <div className="divide-y divide-stone-100">{partners.map(p => <div key={p.id} className="flex items-center justify-between gap-3 p-4"><div className="min-w-0"><p className="truncate text-sm font-semibold text-stone-800">{p.name}</p><p className="mt-1 text-xs text-stone-400">{p.level} · {p.category || "-"}</p></div><div className="flex shrink-0 gap-1.5"><button onClick={() => action("/api/admin/approve", { id:p.id, status:"APPROVED" })} className="inline-flex items-center gap-1 rounded-lg bg-emerald-50 px-2.5 py-2 text-xs font-semibold text-emerald-700 hover:bg-emerald-100"><CheckCircle2 className="h-3.5 w-3.5" /> Setujui</button><button onClick={() => action("/api/admin/approve", { id:p.id, status:"REJECTED", reviewNote:"Ditolak oleh admin" })} className="inline-flex items-center gap-1 rounded-lg bg-red-50 px-2.5 py-2 text-xs font-semibold text-red-600 hover:bg-red-100"><XCircle className="h-3.5 w-3.5" /> Tolak</button></div></div>)}</div> : <Empty text="Tidak ada usulan mitra baru" />}</section><section className="rounded-2xl border border-stone-100 bg-white shadow-sm"><div className="flex items-center justify-between border-b border-stone-100 p-5"><div><h2 className="font-bold text-stone-900">Laporan Kegiatan</h2><p className="mt-0.5 text-xs text-stone-500">Perlu ditinjau dan diverifikasi</p></div><span className="rounded-full bg-blue-50 px-2.5 py-1 text-xs font-bold text-blue-700">{activities.length} pending</span></div>{activities.length ? <div className="divide-y divide-stone-100">{activities.map(a => <div key={a.id} className="flex items-center justify-between gap-3 p-4"><div className="min-w-0"><p className="truncate text-sm font-semibold text-stone-800">{a.title}</p><p className="mt-1 truncate text-xs text-stone-400">{a.partnerName} · {a.submittedBy || "Anonim"}</p>{(a.lecturers?.length || 0) > 0 && <p className="mt-1 truncate text-xs text-emerald-700">Dosen: {a.lecturers!.map(l => l.isLeader ? `${l.name} (Ketua)` : l.name).join(", ")}</p>}{(a.students?.length || 0) > 0 && <p className="mt-1 truncate text-xs text-stone-500">Mahasiswa ({a.students!.length}): {a.students!.map(s => s.name).join(", ")}</p>}</div><div className="flex shrink-0 gap-1.5"><button onClick={() => action("/api/submissions", { id:a.id, status:"APPROVED" })} className="inline-flex items-center gap-1 rounded-lg bg-emerald-50 px-2.5 py-2 text-xs font-semibold text-emerald-700 hover:bg-emerald-100"><CheckCircle2 className="h-3.5 w-3.5" /> Setujui</button><button onClick={() => action("/api/submissions", { id:a.id, status:"REJECTED", reviewNote:"Dokumentasi perlu dilengkapi" })} className="inline-flex items-center gap-1 rounded-lg bg-red-50 px-2.5 py-2 text-xs font-semibold text-red-600 hover:bg-red-100"><XCircle className="h-3.5 w-3.5" /> Tolak</button></div></div>)}</div> : <Empty text="Tidak ada laporan kegiatan baru" />}</section><section className="rounded-2xl border border-stone-100 bg-white shadow-sm"><div className="flex items-center justify-between border-b border-stone-100 p-5"><div><h2 className="font-bold text-stone-900">Usulan Perbaikan Data</h2><p className="mt-0.5 text-xs text-stone-500">Koreksi data mitra dari publik</p></div><span className="rounded-full bg-purple-50 px-2.5 py-1 text-xs font-bold text-purple-700">{edits.length} pending</span></div>{edits.length ? <div className="divide-y divide-stone-100">{edits.map(e => <div key={e.id} className="p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div className="min-w-0"><p className="text-sm font-semibold text-stone-800">{e.partnerName}</p><p className="mt-0.5 text-xs text-stone-400">Diusulkan oleh {e.submitterName} · {new Date(e.createdAt).toLocaleDateString("id-ID")}</p>{e.note && <p className="mt-1 text-xs italic text-stone-500">"{e.note}"</p>}</div><div className="flex shrink-0 gap-1.5"><button onClick={() => action("/api/partner-edits/review", { id:e.id, status:"APPROVED" })} className="inline-flex items-center gap-1 rounded-lg bg-emerald-50 px-2.5 py-2 text-xs font-semibold text-emerald-700 hover:bg-emerald-100"><CheckCircle2 className="h-3.5 w-3.5" /> Terapkan</button><button onClick={() => action("/api/partner-edits/review", { id:e.id, status:"REJECTED", reviewNote:"Perbaikan tidak sesuai" })} className="inline-flex items-center gap-1 rounded-lg bg-red-50 px-2.5 py-2 text-xs font-semibold text-red-600 hover:bg-red-100"><XCircle className="h-3.5 w-3.5" /> Tolak</button></div></div><div className="mt-3 space-y-1.5 rounded-xl bg-stone-50 p-3">{Object.entries(e.changes).map(([field, to]) => <div key={field} className="text-xs"><span className="font-semibold text-stone-600">{FIELD_LABELS[field] || field}:</span> <span className={to === "" ? "italic text-red-600" : "text-stone-400"}>{to === "" ? "(dikosongkan)" : String(to)}</span></div>)}</div></div>)}</div> : <Empty text="Tidak ada usulan perbaikan data" />}</section></div><TeamAdmin />{role === "SUPER_ADMIN" && <AccountAdmin />}<ChangePassword /><section className="rounded-2xl border border-emerald-100 bg-emerald-50/50 p-5"><h2 className="font-bold text-emerald-900">Perlu Perhatian</h2><div className="mt-4 grid gap-3 sm:grid-cols-3"><Notice text={`${stats?.agreementsEndingSoon || 0} perjanjian akan berakhir dalam 90 hari`} /><Notice text={`${stats?.partnersWithoutPic || 0} mitra belum memiliki PIC`} /><Notice text={`${stats?.partnersUnused || 0} mitra belum digunakan dalam 6 bulan`} /></div></section></main></div>;
}
function Stat({ icon:Icon, label, value, color }: { icon:typeof Clock3; label:string; value:number; color:string }) { const c:Record<string,string>={amber:"bg-amber-50 text-amber-700",blue:"bg-blue-50 text-blue-700",yellow:"bg-yellow-50 text-yellow-700",red:"bg-red-50 text-red-700"}; return <div className="flex items-center gap-3 rounded-2xl border border-stone-100 bg-white p-4 shadow-sm"><span className={`flex h-10 w-10 shrink-0 items-center justify-center rounded-xl ${c[color]}`}><Icon className="h-5 w-5" /></span><div><p className="text-xl font-bold text-stone-900">{value}</p><p className="text-xs text-stone-500">{label}</p></div></div> }
function Empty({ text }: { text:string }) { return <div className="px-5 py-12 text-center text-sm text-stone-400"><CheckCircle2 className="mx-auto mb-2 h-7 w-7 text-stone-300" />{text}</div> }
function Notice({ text }: { text:string }) { return <div className="rounded-xl bg-white/70 px-4 py-3 text-sm text-emerald-800">• {text}</div> }
